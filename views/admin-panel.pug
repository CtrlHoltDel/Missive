extends layout 

block content 

  .admin-container
    .admin-container__settings
        h3 Administrator Panel
        a(href="/")   
          button Remove Admin Status
    .users-list 
      h2.user-title User List
      each user in users 
        p #{user.username}